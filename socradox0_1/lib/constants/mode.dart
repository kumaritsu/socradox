enum BottomNavItem{
  topic,
  community,
  add,
  event,
  profile
}

enum UploadType{
  group,
  stream,
}

enum ProfileSettingMenu {
  themeChange,
  signOut,
}

enum WhoCaresMeMode{
  like,
  followings,
  followed,
}

enum TopicMode{
  fromTopic,
  fromProfile,
}

enum TopicCaptionOpenMode{
  fromTopic,
  fromFeed,
}
